// Import Actions
import {  } from './NotesActions';

// Initial State
const initialState = {};

const NotesReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default NotesReducer;
